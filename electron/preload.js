/**
 * Preload minimale: FinanceGuard desktop e' un'app web pura (export statico Next)
 * che non ha bisogno di alcun bridge verso Node. Il file esiste per mantenere
 * contextIsolation attivo senza esporre API privilegiate al renderer.
 */
